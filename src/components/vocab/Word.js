import React from 'react';

const Word = ({ word: name, definition, type }) => {
  console.log(name);
  return (
    <div>
      {/* <p>{name}</p>
      <p>{definition}</p>
      <type>{type}</type> */}
    </div>
  );
};

export default Word;
